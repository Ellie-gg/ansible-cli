# SETUP - This module is automatically called by playbooks to gather useful variables about remote hosts that can be used in playbooks
#ansible -m setup -i hosts node2

#ansible -m command -a "df -h" -i hosts hosts.txt -u usuario

cat /ansible/hosts
node1 ansible_ssh_host=192.168.0.10 ansible_ssh_user=usuario ansible_ssh_pass=senhadousuario
